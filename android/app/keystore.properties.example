# Keystore properties for signing your Android release builds
# 
# IMPORTANT: 
# 1. Copy this file to 'keystore.properties' (without .example)
# 2. Fill in your actual values
# 3. NEVER commit keystore.properties to version control!
#
# After creating your keystore with:
# keytool -genkey -v -keystore shinecycle-release.jks -alias shinecycle -keyalg RSA -keysize 2048 -validity 10000
#
# Fill in the values below:

storeFile=shinecycle-release.jks
storePassword=YOUR_KEYSTORE_PASSWORD_HERE
keyAlias=shinecycle
keyPassword=YOUR_KEY_PASSWORD_HERE
